# Machine-specific overrides — never commit secrets or local tweaks
**/settings.local.json
**/mcp.local.json
**/*.local.md

# Editor / OS noise
.DS_Store
*.swp
.idea/

# Local docs — plans, solutions, and reports are project-local artifacts, not global config
docs/
